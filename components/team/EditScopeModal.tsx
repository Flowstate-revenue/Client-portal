// DEPRECATED -- superseded by EditManagerModal.tsx, which edits
// name/phone alongside the single is_super_manager flag in one action
// (the two independent scopes this component toggled no longer exist).
//
// Nothing imports this file anymore. Couldn't delete it (locked by the
// running dev server in the build sandbox) -- safe to delete manually.
export {}
